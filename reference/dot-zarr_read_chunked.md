# Read multi-chunk zarr v2 numeric array

Read multi-chunk zarr v2 numeric array

## Usage

``` r
.zarr_read_chunked(
  store_or_path,
  array_rel,
  meta,
  dtype_info,
  shape,
  chunks,
  n_chunks_per_dim,
  compressor
)
```
