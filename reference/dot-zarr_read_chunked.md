# Read multi-chunk zarr v2 numeric array

Read multi-chunk zarr v2 numeric array

## Usage

``` r
.zarr_read_chunked(
  array_dir,
  meta,
  dtype_info,
  shape,
  chunks,
  n_chunks_per_dim,
  compressor
)
```
