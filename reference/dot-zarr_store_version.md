# Detect zarr store format version

Detect zarr store format version

## Usage

``` r
.zarr_store_version(store_path)
```

## Arguments

- store_path:

  Path to zarr store directory

## Value

2L or 3L
