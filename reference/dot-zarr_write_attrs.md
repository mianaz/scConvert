# Write zarr attributes to .zattrs file

Write zarr attributes to .zattrs file

## Usage

``` r
.zarr_write_attrs(dir, attrs)
```

## Arguments

- dir:

  Directory containing the zarr node

- attrs:

  Named list of attributes
