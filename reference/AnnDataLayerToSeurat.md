# Map AnnData layer name to Seurat layer name

Map AnnData layer name to Seurat layer name

## Usage

``` r
AnnDataLayerToSeurat(name)
```

## Arguments

- name:

  AnnData layer name

## Value

Seurat layer name
