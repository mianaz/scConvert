# Encode R factor as AnnData categorical

Encode R factor as AnnData categorical

## Usage

``` r
EncodeCategorical(x)
```

## Arguments

- x:

  A factor (or coercible to factor)

## Value

A list with `codes` (0-based integer) and `categories` (character vector
of levels)
