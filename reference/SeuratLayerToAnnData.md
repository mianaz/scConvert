# Map Seurat layer name to AnnData layer name

Map Seurat layer name to AnnData layer name

## Usage

``` r
SeuratLayerToAnnData(name)
```

## Arguments

- name:

  Seurat layer name

## Value

AnnData layer name
