# Read AnnData index from obs or var group

Read AnnData index from obs or var group

## Usage

``` r
.zarr_read_anndata_index(store_path, group)
```

## Arguments

- store_path:

  Path to zarr store

- group:

  "obs" or "var"

## Value

Character vector of index values, or NULL
