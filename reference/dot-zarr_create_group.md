# Create a zarr v2 group directory with metadata

Create a zarr v2 group directory with metadata

## Usage

``` r
.zarr_create_group(dir, attrs = NULL)
```

## Arguments

- dir:

  Directory path for the group

- attrs:

  Named list of attributes (written to .zattrs)
