# Build chunk file path for zarr v2

Build chunk file path for zarr v2

## Usage

``` r
.zarr_chunk_path_v2(array_dir, chunk_coords)
```

## Arguments

- array_dir:

  Array directory path

- chunk_coords:

  Integer vector of chunk coordinates (0-based)

## Value

File path to chunk
