# Read a zarr v2 numeric array from disk

Read a zarr v2 numeric array from disk

## Usage

``` r
.zarr_read_numeric(store_path, rel_path)
```

## Arguments

- store_path:

  Path to zarr store

- rel_path:

  Relative path to array within store

## Value

Numeric vector or matrix
