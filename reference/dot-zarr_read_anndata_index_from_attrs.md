# Read an anndata index given pre-read attrs

Read an anndata index given pre-read attrs

## Usage

``` r
.zarr_read_anndata_index_from_attrs(store_path, attrs)
```

## Arguments

- store_path:

  Path to zarr group

- attrs:

  Pre-parsed .zattrs list

## Value

Character vector of index values
