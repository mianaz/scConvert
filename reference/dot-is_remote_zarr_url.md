# Detect remote zarr URL

Detect remote zarr URL

## Usage

``` r
.is_remote_zarr_url(x)
```

## Arguments

- x:

  A character string (path or URL)

## Value

TRUE if x looks like a remote zarr URL (s3://, gs://, http(s)://)
