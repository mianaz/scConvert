# Check if h5Seurat file uses V5 structure

Check if h5Seurat file uses V5 structure

## Usage

``` r
IsV5Structure(h5_file)
```

## Arguments

- h5_file:

  Path to h5Seurat file or h5Seurat object

## Value

Logical indicating if file uses V5 structure
