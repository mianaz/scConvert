# Map AnnData reduction name to Seurat key

Map AnnData reduction name to Seurat key

## Usage

``` r
AnnDataReductionKey(name)
```

## Arguments

- name:

  Reduction name (e.g., "pca", "umap", "tsne")

## Value

A Seurat-style key string (e.g., "PC\_", "UMAP\_")
