# List children of a zarr group

List children of a zarr group

## Usage

``` r
.zarr_list_children(store_path, rel_path = "")
```

## Arguments

- store_path:

  Path to zarr store

- rel_path:

  Relative path to group within store

## Value

Character vector of child names
