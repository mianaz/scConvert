% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ReadWriteSOMA.R
\name{writeSOMA}
\alias{writeSOMA}
\title{Write a Seurat object to TileDB-SOMA format}
\usage{
writeSOMA(object, uri, measurement = NULL, overwrite = FALSE, verbose = TRUE)
}
\arguments{
\item{object}{A Seurat object}

\item{uri}{Output URI (local path or cloud URI such as \code{tiledb://...}
or \code{s3://...})}

\item{measurement}{Name for the primary measurement. If \code{NULL}
(default), uses \code{DefaultAssay(object)}.}

\item{overwrite}{If \code{TRUE}, remove any existing SOMA at \code{uri}
before writing. Default is \code{FALSE}.}

\item{verbose}{Show progress messages}
}
\value{
Invisibly returns \code{uri}
}
\description{
Writes a Seurat object to a TileDB-SOMA experiment. Uses the
\code{tiledbsoma::write_soma()} function from the \pkg{tiledbsoma} package,
which handles the full conversion of assays, metadata, embeddings, and
graphs.
}
\examples{
\dontrun{
library(scConvert)
library(Seurat)

# Write a Seurat object to SOMA
writeSOMA(seurat_obj, uri = "output_soma")

# Overwrite existing
writeSOMA(seurat_obj, uri = "output_soma", overwrite = TRUE)
}

}
\seealso{
\code{\link{readSOMA}}, \code{\link{scConvert}}
}
